# =============================================================================
# Hyprland config (base)
# Autor: Gabriel Rocha (rag) + Codex
# Data: 2026-03-12
#
# Referenciado por `desktop/hyprland/user.nix` via `extraConfig`.
# =============================================================================

# Monitor (generico — ajuste por host via kanshi ou monitor=)
monitor=,preferred,auto,1

# =============================================================================
# Autostart — servicos de sessao
# Todos via `uwsm app --` para integracao correta com UWSM + systemd-user.
# NOTA: servicos que usam systemd.enable (waybar, cliphist, kanshi, swaync)
#       sao gerenciados automaticamente pelo systemd-user apos o graphical-session.target;
#       NAO devem ser duplicados aqui.
# NOTA: o handoff da sessao para systemd-user ja vem de UWSM + Home Manager;
#       nao reiniciar `hyprland-session.target` manualmente aqui.
# NOTA: quando um shell fornece launcher/notificacoes, os binds chamam wrappers
#       genericos do projeto e evitam duplicar a UX.
# =============================================================================
exec-once = uwsm app -- hypridle
exec-once = uwsm app -- nm-applet --indicator

# =============================================================================
# Input
# =============================================================================
input {
  kb_layout   = br
  kb_variant  = abnt2
  repeat_delay = 250
  repeat_rate  = 40
  follow_mouse = 1
  mouse_refocus = false
  sensitivity  = 0
  accel_profile = flat

  touchpad {
    natural_scroll    = true
    disable_while_typing = true
    tap-to-click      = true
    scroll_factor     = 0.5
  }
}

# =============================================================================
# Geral
# =============================================================================
general {
  gaps_in   = 4
  gaps_out  = 8
  border_size = 2
  col.active_border   = rgba(7aa2f7ff) rgba(bb9af7ff) 45deg
  col.inactive_border = rgba(414868aa)
  layout = master
  resize_on_border = true
  allow_tearing    = false   # true somente em host com tearing explicito (Glacier)
}

# =============================================================================
# Decoracao
# =============================================================================
decoration {
  rounding = 8

  blur {
    enabled = false
    popups   = false
  }

  shadow {
    enabled      = false
    range        = 8
    render_power = 3
    color        = rgba(1a1a2eee)
  }

  dim_inactive   = false
  dim_strength   = 0.0

  active_opacity   = 1.0
  inactive_opacity = 1.0
}

# =============================================================================
# Animacoes
# =============================================================================
animations {
  enabled = true

  animation = windows, 1, 3, default
  animation = fade, 1, 3, default
  animation = border, 0
  animation = workspaces, 0
  animation = layers, 0
}

# =============================================================================
# Layout
# =============================================================================
master {
  orientation = left
  mfact        = 0.55
  new_status   = slave
}

dwindle {
  pseudotile     = true
  preserve_split = true
}

# =============================================================================
# Misc
# =============================================================================
misc {
  force_default_wallpaper    = 0
  disable_hyprland_logo      = true
  disable_splash_rendering   = true
  vrr                        = 1       # VRR/FreeSync quando disponivel
  vfr                        = true    # reduz trabalho de render em hardware fraco/idle
  mouse_move_enables_dpms    = true
  key_press_enables_dpms     = true
  focus_on_activate          = true
  animate_manual_resizes     = false
  enable_swallow             = true
  swallow_regex              = ^(tilix|com\.gexperts\.Tilix|alacritty|kitty|foot)$
}

# =============================================================================
# Gestos (touchpad)
# =============================================================================
gestures {
  workspace_swipe_distance     = 300
  workspace_swipe_cancel_ratio = 0.3
}

# Gesto de 3 dedos para trocar workspace (API atual do Hyprland 0.53+)
gesture = 3, horizontal, workspace

# =============================================================================
# Env vars exportadas para processos filhos (complementa systemd via --all)
# =============================================================================
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

# =============================================================================
# Atalhos (custom — workspaces, janelas, launcher, clipboard, codex)
# =============================================================================
$mainMod = SUPER

# --- Janela ---------------------------------------------------------------
bind = $mainMod, Q, killactive,
bind = $mainMod, F, togglefloating,
bind = $mainMod CTRL, M, fullscreen, 0
bind = $mainMod, Return, layoutmsg, swapwithmaster
bind = $mainMod, R, layoutmsg, orientationcycle
bind = $mainMod, Tab, exec, uwsm app -- kryonix-window-menu
bind = $mainMod SHIFT, M, movetoworkspacesilent, special
bind = $mainMod SHIFT, P, togglespecialworkspace
bind = CTRL ALT, Q, exit

# Centralizar janela
bind = CTRL ALT, C, centerwindow

# --- Foco (hjkl) -----------------------------------------------------------
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# --- Redimensionar ---------------------------------------------------------
bind = $mainMod SHIFT, left,  resizeactive, -50 0
bind = $mainMod SHIFT, right, resizeactive,  50 0
bind = $mainMod SHIFT, up,    resizeactive,  0 -50
bind = $mainMod SHIFT, down,  resizeactive,  0  50

# --- Mouse -----------------------------------------------------------------
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# --- Workspaces ------------------------------------------------------------
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll para navegar
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up,   workspace, e-1

# --- Apps rapidos ----------------------------------------------------------
bind = $mainMod, E, exec, uwsm app -- dolphin
bind = $mainMod, T,          exec, uwsm app -- warp-terminal
bind = $mainMod, C,          exec, uwsm app -- kryonix-calc-menu
bind = $mainMod SHIFT, B,      exec, uwsm app -- brave
bind = $mainMod SHIFT, F,      exec, uwsm app -- dolphin
bind = $mainMod SHIFT, G,      exec, uwsm app -- codex
bind = $mainMod CTRL, E,       exec, uwsm app -- filelight

# --- Launcher / Shell ------------------------------------------------------
bind = $mainMod, A,     exec, uwsm app -- kryonix-launcher
bind = $mainMod SHIFT, A, exec, uwsm app -- kryonix-runner
bind = $mainMod, D,     exec, uwsm app -- kryonix-quick-actions
bind = $mainMod, G,     exec, uwsm app -- kryonix-shell-dashboard
bind = ALT SHIFT, V,    exec, uwsm app -- kryonix-clipboard-menu
bind = $mainMod, V,     exec, uwsm app -- kryonix-clipboard-menu
bind = $mainMod, N,     exec, kryonix-shell-notifications

# --- Utilidades ------------------------------------------------------------
bind = $mainMod SHIFT, S,    exec, kryonix-ipc picker openFreeze
bind = $mainMod CTRL,  S,    exec, uwsm app -- kryonix-screenshot edit-output
bind = $mainMod SHIFT, R,    exec, $HOME/.local/bin/screen-recorder
bind = $mainMod CTRL,  R,    exec, uwsm app -- kryonix-record-menu
bind = $mainMod, L,          exec, kryonix-shell-lock
bind = CTRL ALT, L,          exec, kryonix-shell-lock
bind = $mainMod, M,          exec, kryonix-ipc mpris playPause
bind = $mainMod, X,          exec, uwsm app -- kryonix-power-menu
bind = $mainMod, O,          exec, uwsm app -- kryonix-audio-menu
bind = $mainMod, W,          exec, uwsm app -- kryonix-network-menu
bind = $mainMod SHIFT, C,    exec, hyprpicker -a
bind = $mainMod SHIFT, Backspace, exec, kryonix-shell-notifications-clear
bind = CTRL ALT, P,          exec, gnome-pomodoro --start-stop
bind = ALT SHIFT, 2,         exec, $HOME/.local/bin/ocr

# Print Screen (Wayland)
# - Print: seleção (copia)
# - Super+Print: tela inteira (salva + copia)
# - Super+Shift+Print: janela ativa (salva + copia)
bind = , Print,              exec, uwsm app -- kryonix-screenshot copy-area
bind = $mainMod, Print,      exec, uwsm app -- kryonix-screenshot copysave-screen
bind = $mainMod SHIFT, Print, exec, uwsm app -- kryonix-screenshot copysave-active

# --- Brilho ----------------------------------------------------------------
bindel = , XF86MonBrightnessUp,   exec, uwsm app -- kryonix-brightness up
bindel = , XF86MonBrightnessDown, exec, uwsm app -- kryonix-brightness down
bindel = SHIFT, XF86MonBrightnessUp,   exec, uwsm app -- kryonix-kbd-brightness up
bindel = SHIFT, XF86MonBrightnessDown, exec, uwsm app -- kryonix-kbd-brightness down

# --- Volume ----------------------------------------------------------------
bind = , XF86AudioRaiseVolume, exec, pamixer --allow-boost --set-limit 130 --increase 5
bind = , XF86AudioLowerVolume, exec, pamixer --allow-boost --set-limit 130 --decrease 5
bind = , XF86AudioMute,        exec, pamixer --toggle-mute
bind = , XF86AudioMicMute,     exec, pamixer --default-source --toggle-mute
bind = SHIFT, XF86AudioRaiseVolume, exec, pamixer --increase 5 --default-source
bind = SHIFT, XF86AudioLowerVolume, exec, pamixer --decrease 5 --default-source
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPause, exec, playerctl pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous
